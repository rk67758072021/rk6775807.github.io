#include<stdio.h>
#include<stdlib.h>

int main(){
    struct node{
        int data;
        struct node* next;
    };
    struct node* newnode, *head, *temp;

    newnode=(struct node*)malloc(sizeof(struct node));
    head=newnode;
    if(head!=0)
    {
        
    }
}